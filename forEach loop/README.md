# forEach Loop

This folder is about the JavaScript `forEach()` method.

`forEach()` executes a function once for each array element.

Example:
```js
const fruits = ["apple", "banana", "mango"];

fruits.forEach((fruit) => {
  console.log(fruit);
});
```

Use `forEach()` when you want to perform an action for every element in an array.

# While Loop

This folder is about the `while` loop in JavaScript.

The `while` loop repeats code as long as a condition stays true.

Example:
```js
let i = 0;
while (i < 5) {
  console.log(i);
  i++;
}
```

Use `while` when you need to repeat until a condition changes.

Scope

This folder is about scope in JavaScript.

Scope means where a variable can be used.

Main types:
- Global scope
- Function scope
- Block scope

Example:
let a = 10;

function demo() {
  let b = 20;
  console.log(a + b);
}

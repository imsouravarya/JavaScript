Date

This folder is about working with dates in JavaScript.

You can create dates, get the current date, and format them.

Common concepts:
- new Date()
- getFullYear()
- getMonth()
- getDate()
- toLocaleDateString()

Example:
const today = new Date();
console.log(today);

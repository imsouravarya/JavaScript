Conversion Operations

This folder shows how values can be changed from one type to another.

Common conversions:
- Number(value)
- String(value)
- Boolean(value)

Example:
let a = 100;
let b = String(a);
console.log(b);

Practice these examples to understand how JavaScript converts values.

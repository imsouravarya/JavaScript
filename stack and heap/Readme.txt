
stack and heap 

stack = Premitive
heap = Non-premitive

means 

in stack(premitive),(WE GET COPY HERE)

we dont change the actual data, we change the copy of 
data we just assigned to some other variable

example: we will see


In Heap(Non-premitive), (WE GET REFERENCE HERE)

we change the actual data, and when we call them later, we get the 
updated/changed data.

example: we will see


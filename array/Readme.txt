Array

This folder is about arrays in JavaScript.

Arrays are used to store multiple values in one variable.

Common concepts:
- creating an array
- accessing elements
- adding and removing items
- array methods like push(), pop(), map(), filter()

Example:
let numbers = [1, 2, 3, 4];
console.log(numbers[0]);

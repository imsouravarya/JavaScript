Arrow Function

This folder is about arrow functions in JavaScript.

Arrow functions provide a shorter syntax for writing functions.

Example:
const add = (a, b) => a + b;
console.log(add(2, 3));

They are commonly used in modern JavaScript.

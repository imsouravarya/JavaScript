Objects

This folder is about objects in JavaScript.

Objects are used to store data in key-value pairs.

Common concepts:
- object literal
- accessing properties
- adding properties
- nested objects

Example:
const user = { name: "Sourav", age: 20 };
console.log(user.name);
